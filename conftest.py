"""
conftest.py — root-level pytest configuration.

Placing this file at the project root tells pytest to add this directory
to sys.path, making `env`, `graders`, and `tasks` importable in all tests.
"""
